services:
  rfit:
    build:
      context: ${REPO}
      dockerfile: Dockerfile
    restart: no
    image: rfit
    container_name: rfit
    volumes:
      - ./:/workspace
    networks:
      - shared-net

networks:
  shared-net:
    external: true
    name: subspace